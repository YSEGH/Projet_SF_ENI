{% extends 'base.html.twig' %}
{% block css %}
    <link rel="stylesheet" href="{{ asset('style/article_details.css') }}" />
{% endblock %}

{% block body %}
<div class="item details">
    <div class="item_header">
        <img class="item_link" src="{{ asset(article.image) }}" alt="{{ article.slug }} image" />
    </div>
    <div class="text-container">
        <ul>
            <li class="name">{{ article.name }}</li>
            <li class="category">
        <a href="{{path('app_list', { categorie: article.category })}}">{{ article.category }}</a>
            </li>
            <li class="description">{{ article.description }}</li>
            <li class="price">{{ article.price }}€</li>
        </ul>
        {{ form_start(form) }}
        <div class="item_link">
            {{ form_label(form.quantity) }}
            {{ form_widget(form.quantity) }}
            {{ form_widget(form.add) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
